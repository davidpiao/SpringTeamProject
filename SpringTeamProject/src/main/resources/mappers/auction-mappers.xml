<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Auction">

<select id="getUser" resultType="com.com.myapp.user.UserVO">
select ID, username from user where ID=#{ID} and password=#{password}
</select>

<select id="getSid" resultType="com.com.myapp.user.UserVO">
select userid from user where ID=#{ID}
</select>

<insert id="insertUser">
insert into user (ID, username, password)
values
(#{ID} , #{username} , #{password})
</insert>

</mapper>